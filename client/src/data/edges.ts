import type { Edge } from "@xyflow/react";

const initialEdges: Edge[] = [
    {
        id: "1-2",
        source: "0",
        target: "1",
        animated: true
    }
]

export default initialEdges;